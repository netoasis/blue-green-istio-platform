apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.global.appName }}-canary"
  namespace: "{{ .Values.global.namespace }}"
spec:
  selector:
    app: "{{ .Values.global.appName }}"
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5678
  type: ClusterIP