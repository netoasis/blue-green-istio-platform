apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.global.appName }}-preview"
  namespace: "{{ .Values.global.namespace }}"
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5678
  type: ClusterIP