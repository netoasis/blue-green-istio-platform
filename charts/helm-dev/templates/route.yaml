apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "{{ .Values.global.appName }}-route"
  namespace: "{{ .Values.global.namespace }}"
spec:
  parentRefs:
    - name: {{ .Values.global.appName }}-gateway

  hostnames:
    - {{ .Values.global.host }}

  rules:
    - backendRefs:
        - name: {{ .Values.global.appName }}-stable
          port: 80
          weight: 100

        - name: {{ .Values.global.appName }}-canary
          port: 80
          weight: 0