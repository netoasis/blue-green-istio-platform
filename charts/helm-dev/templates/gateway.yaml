apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: "{{ .Values.global.appName }}-gateway"
  namespace: "{{ .Values.global.namespace }}"
spec:
  gatewayClassName: nginx

  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: {{ .Values.global.host }}
